{
  "name": "snsw_int_task",
  "version": "1.0.0",
  "description": "Core Automation Framework for UI and API testing",
  "main": "index.js",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test tests/ui/",
    "test:api": "playwright test tests/api/",
    "clean:reports": "rimraf test-results playwright-report",
    "install-browsers": "playwright install"
  },
  "keywords": ["automation", "playwright", "api", "testing"],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@types/node": "^25.7.0",
    "rimraf": "^5.0.0"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "dotenv": "^16.3.0"
  }
}
